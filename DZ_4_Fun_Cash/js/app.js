function calculateCashback(amount, percent) {

    return cashback = amount * percent / 100;
}

const amount = 1000;
const percent = 50;

const result = calculateCashback(amount, percent);
console.log(result);